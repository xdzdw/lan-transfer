# Home Layout Verification

The home page now uses a scrollable vertical shell for short viewports. At 1280x600, the mode-switch button is fully visible at the bottom of the viewport instead of being clipped. At 1280x720, the page remains vertically centered with the full button visible. At 375x812, the mobile layout remains intact and the switch button is fully visible.

TypeScript compilation passed and the full Vitest suite passed: 7 test files and 62 tests.
